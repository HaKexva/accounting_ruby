#!/bin/bash -e

# Prepare DB before web boot (Railway CMD is ./bin/start-web, not bare rails server).
if [ "${1}" = "./bin/start-web" ] || { [ "${@: -2:1}" = "./bin/rails" ] && [ "${@: -1:1}" = "server" ]; }; then
  ./bin/rails db:prepare
fi

exec "${@}"
